"""Integration tests: the real host toolchain, per Rule 1 §6.

Real ``adb``, real ``avdmanager``, real AVDs in the real ``~/.android/avd`` and
one real headless emulator boot. Nothing here is faked, mocked, patched or
stubbed.
"""
