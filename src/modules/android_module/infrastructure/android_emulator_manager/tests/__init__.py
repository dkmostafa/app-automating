"""Tests for the android_emulator_manager component.

One test file per source file (Rule 2): ``parsing.py`` is covered by
``unit/test_parsing.py``, ``manager.py`` by ``integration/test_manager.py``, and
so on. A source file may appear in both ``unit/`` and ``integration/`` when it
has both a pure surface and a host-dependent one -- ``config.py`` does.
"""
